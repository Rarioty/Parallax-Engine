#ifndef PARALLAX_HASH_HPP_GUARD
#define PARALLAX_HASH_HPP_GUARD

#include <Parallax/Types.hpp>
#include <string>

namespace Parallax
{
    namespace Maths
    {
        U32 crc32(const char* name, U32 len);
    }
}

#endif
